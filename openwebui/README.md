### OpenWeb UI and Searxng

OpenWeb UI is a GUI that can be used to interact with the ollama AI model server.

This is docker compose example for running openweb ui and searxng together.

Assumes nomic-embed-text model is installed in ollama.
